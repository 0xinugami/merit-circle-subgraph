{
  "name": "merit-circle",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ merit-circle",
    "create-local": "graph create --node http://localhost:8020/ merit-circle",
    "remove-local": "graph remove --node http://localhost:8020/ merit-circle",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 merit-circle"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.23.0",
    "@graphprotocol/graph-ts": "0.22.1"
  }
}
